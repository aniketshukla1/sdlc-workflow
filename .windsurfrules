# .windsurfrules — SDLC workflow (Jira + GitLab, trunk-based)

Router: `AGENTS.md` is the source of truth. Skills: `skills/jira-autopilot/SKILL.md`, `skills/mr-review/SKILL.md` (run `./scripts/install-skills.sh --all` for upstream engineering skills). Commands: `commands/*.toml` (autopilot, intent, spec, planning, build, test, review, mr-review, ship).

- Jira work: paste key (`PROJ-123`) or summary+description → clarify to ~95% (≤3 questions/round, assumptions listed) → `intent/INTENT-<KEY>.md` (accept) → branch `PROJ-123-slug` (key first, no feat/ prefix) → spec `docs/specs/SPEC-<KEY>.md` (approval gate, skills + concerns + provenance) → plan `tasks/<KEY>-plan.md` (approval gate, plan mode, diff-vs-plan) → build TDD locally (commits `KEY [Type] <feat|fix|...>: ...`) → `./scripts/ui-verify.sh` (human browser pass = push gate) → `./scripts/publish.sh` (first push) → MR `[KEY][Type]` + Jira label.
- Reviews: Jira-AC traceability first, then five-axis gates per `REVIEW.md`; verdict is Approve / Request changes / Comment.
- Never push before the UI pass. Never weaken tests/linters to get green. Never emit secrets into specs/Jira/MR.
