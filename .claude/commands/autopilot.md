---
description: Jira paste to GitLab MR autopilot — intent, spec, plan, build, verify, publish
---

Invoke the `jira-autopilot` skill. Accept a pasted Jira key (`PROJ-123`) OR a summary + description (+ type) and detect which mode applies — if the shape is invalid, stop and show the expected shape instead of guessing. Prefer Jira/GitLab MCP tools when configured, else REST with `JIRA_HOST`/`JIRA_EMAIL`/`JIRA_API_TOKEN` and `GITLAB_HOST`/`GITLAB_TOKEN` (print the export block and stop if creds are missing). Fetch or create the Jira issue from `templates/jira/*.md`, clarify per `docs/clarification-protocol.md` (max 3 questions per round, assumptions listed) until ~95%, then orchestrate: `new-issue.sh` branch/worktree → `/intent` (accept) → `/spec` (stop for approval, skills + concerns + provenance) → `/plan` (stop for approval, plan mode, diff-vs-plan) → `/build` TDD locally → `/test` + `ui-verify.sh` (human browser pass is the push gate) → `publish.sh` first push → open GitLab MR `[KEY][Type]` + Jira label + filled template + test evidence → Jira `#in-review` comment. Never push before the UI pass, never auto-merge/close, never paste secrets into specs/Jira/MR.
