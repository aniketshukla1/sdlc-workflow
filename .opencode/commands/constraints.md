---
description: Set the project quality bar once
---

Invoke `constraint-driven-development`. Interview for thresholds, write `CONSTRAINTS.md` (from `CONSTRAINTS.md.example`), place cheap checks early (pre-commit/CI) and expensive checks late. Never weaken the bar to get green.
