description = "Web performance audit, measure first"

prompt = """
Invoke `performance-optimization` (web-performance-auditor persona, Quick/Deep modes). Measure Core Web Vitals + bundle + queries before optimizing. Web apps only.
"""
